/**
 * Engine SPI and default implementation. Frameworks can adapt this layer
 * with decorators, listeners, and metrics without introducing dependencies
 * into the core domain model.
 *
 * @author gokalp
 */
package com.flowsentinel.core.engine;